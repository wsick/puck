namespace puck {
    export enum FillRule {
        evenodd = 0,
        nonzero = 1,
    }

    export enum PenLineJoin {
        miter = 0,
        bevel = 1,
        round = 2,
    }

    export enum PenLineCap {
        flat = 0,
        square = 1,
        round = 2,
        triangle = 3,
    }
}